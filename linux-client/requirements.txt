websockets>=12.0

# Real input injection — needs to run on an actual Linux machine with
# /dev/uinput access. Not installable/testable inside a plain container.
evdev>=1.7.1

# GUI (gui.py) — usually already available as a system package rather
# than pip-installable; see README.md for the exact package name per
# distro. Only needed if you want the app; client.py's CLI form works
# without it.
# tkinter

# QR pairing (also gui.py) — desktop generates a QR code the phone
# scans. Debian/Ubuntu/Mint: python3-qrcode, python3-pil, python3-pil.imagetk
# qrcode
# Pillow

# Dev/test only
pytest>=8.0
